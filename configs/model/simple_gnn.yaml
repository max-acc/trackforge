# ----------------------------------------------------------------------------------------------------------------------
# Configuration data the simple edge classifier model.
# ----------------------------------------------------------------------------------------------------------------------

default:
  name: "SimpleEdgeClassifier"

model:
  node_features:  4
  hidden_dim:     64
  num_layers:     20
  