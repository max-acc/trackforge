# ----------------------------------------------------------------------------------------------------------------------
# Configuration parameters for training
# ----------------------------------------------------------------------------------------------------------------------

training:
  training_split:   0.8
  validation_split: 0.0
  batch_size:       64
  max_epochs:       100
  min_epochs:       20
  learning_rate:    0.001
  converger:        "RelLoss"